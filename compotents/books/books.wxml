<!--compotents/books/books.wxml-->
<view class="books">
  <view class="numbers" wx:if="{{books.length}}">
    命中{{books.length}}条记录
  </view>
  <view class="bookItem" wx:for="{{books}}" wx:key="index" data-recPach="{{item.recPath}}" bindtap="getDetail">
      <view class="text"><text class="text1">{{item.no}}</text>{{item.name}}</view>
      <view class="text2">{{item.recPath}}</view>
  </view> 
</view>

 